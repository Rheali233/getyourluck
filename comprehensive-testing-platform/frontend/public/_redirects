# API Proxy to backend
/api/*  https://selfatlas-backend-prod.cyberlina.workers.dev/api/:splat  301

# SPA fallback - MUST be last rule
# Note: Static files (/assets/*, /css/*, /js/*) are served BEFORE this rule
# Cloudflare Pages checks if file exists first, then applies redirects
/*  /index.html  200
