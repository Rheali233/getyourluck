name: Deploy Frontend to Cloudflare Pages (Staging)

on:
  push:
    branches: [ staging ]
    paths:
      - 'comprehensive-testing-platform/frontend/**'
      - 'comprehensive-testing-platform/shared/**'
      - '.github/workflows/cloudflare-pages-staging.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deploy even if no changes'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: read
  deployments: write
  id-token: write

jobs:
  deploy-staging:
    name: Build and Deploy to Staging
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: comprehensive-testing-platform/frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: comprehensive-testing-platform/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Verify build output
        run: |
          echo "📦 Checking dist directory..."
          ls -la dist/ | head -20
          echo ""
          echo "📁 Checking functions directory..."
          ls -la dist/functions/ 2>/dev/null || echo "⚠️ functions directory not found"

      - name: Install Wrangler CLI
        run: npm install -g wrangler@latest

      - name: Deploy to Cloudflare Pages (Staging)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "📋 Current directory: $(pwd)"
          echo "📋 Dist directory exists: $(test -d dist && echo 'yes' || echo 'no')"
          echo "📋 Functions directory exists: $(test -d dist/functions && echo 'yes' || echo 'no')"
          wrangler pages deploy dist \
            --project-name getyourluck-testing-platform \
            --branch staging

      - name: Verify deployment
        run: |
          echo "✅ Staging deployment completed"
          echo "🔗 Deployed to: https://staging.getyourluck-testing-platform.pages.dev"
          echo "🔗 Or preview URL: https://[hash].getyourluck-testing-platform.pages.dev"

